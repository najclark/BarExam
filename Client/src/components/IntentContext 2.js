import { createContext } from "react";


export const IntentContext = createContext(null);

export const StartIntent = "Start";
export const GameIntent = "Game";
export const AddBarIntent = "AddBar";
